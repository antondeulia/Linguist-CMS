// import z from "zod"

// export const SegmentJsonSchema = z.object({
// 	text: z.string(),
// 	translation: z.string(),
// })

// export const ExerciseJsonSchema = z.object({
// 	rawText: z.string(),
// 	segments: z.array(SegmentJsonSchema),
// })

// export const UnitJsonSchema = z.object({
// 	name: z.string(),
// 	exercises: z.array(ExerciseJsonSchema),
// })

// export const TrackJsonSchema = z.object({
// 	name: z.string(),
// 	units: z.array(UnitJsonSchema),
// })

// export const SectionJsonSchema = z.object({
// 	name: z.string(),
// 	tracks: z.array(TrackJsonSchema),
// })

// export const CourseJsonSchema = z.object({
// 	name: z.string(),
// 	sections: z.array(SectionJsonSchema),
// })
